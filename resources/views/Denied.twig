{% extends "Index.twig" %}
{% block content %}
	<div class="tokenly-access-denied">
		<h1>Access Denied</h1>
		<div class="note">{{ verdict.note }}</div>
		{% if verdict.reports %}
			<div class="rule-groups">
				{% for key, rule_group in rule_groups %}
					<div class="rule-group">
						<div class="rules">
							{% for key, rule in rule_group %}
								<div class="rule">
									<div class="attributes" >
										{% if key > 0 %}
											<div class="group-operator">{{ rule.stack_op }}</div>
										{% endif %}
										<div class="expression">
											<span class="asset">{{ rule.asset }}</span>
											<span class="op">{{ rule.op }}</span>
											<span class="quantity">{{ rule.quantity }}</span>
										</div>
									</div>
								</div>
							{% endfor %}
						</div>
						<div class="status {{verdict.reports[key] | lower}}">{{ verdict.reports[key] }}</div>
					</div>
				{% endfor %}
			</div>
		{% endif %}
		<a href="{{back_url}}" class="tokenly-component-button back-button">Return</a>

	</div>
{% endblock %}
