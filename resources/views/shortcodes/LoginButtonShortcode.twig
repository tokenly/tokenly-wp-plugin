{% from 'components/LoginButtonComponent.twig' import login_button_component %}
{% if can_connect and is_logged_in == false %}
	{{ login_button_component( logo, label, url, namespace ) }}
{% endif %}
